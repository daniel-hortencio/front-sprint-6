import React from 'react';
import { LoginTemplate } from '../../templates/Login';

const Home: React.FC = () => (
  <LoginTemplate>
    <h1>Página não encontrada</h1>
  </LoginTemplate>
);

export default Home;
